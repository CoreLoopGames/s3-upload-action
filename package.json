{
  "name": "s3-upload-action",
  "version": "1.0.0",
  "description": "A GitHub Action to upload files to S3 using AWS SDK's upload() method.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "aws-sdk": "^2.1350.0",
    "@actions/core": "^1.10.0"
  }
}
